# Bust Forge — Python 3.12 runtime dependencies.
# Direct dependencies with compatible ranges. After a verified install,
# `pip freeze` the environment to requirements.lock for exact reproduction.
#
# Install with:
#   py -3.12 -m venv .venv
#   .venv\Scripts\python -m pip install -U pip
#   .venv\Scripts\python -m pip install -r requirements.txt

# --- Core ---
numpy>=1.26,<3
PyYAML>=6.0
Pillow>=10.0
opencv-python-headless>=4.9
scipy>=1.11
tqdm>=4.66
requests>=2.31
psutil>=5.9
filelock>=3.13

# --- Torch (PyPI Windows wheels bundle CUDA 12.x runtime) ---
torch>=2.3
torchvision>=0.18

# --- 3D / mesh handling ---
trimesh>=4.0
numpy-stl>=3.0
open3d>=0.18
manifold3d>=2.4
PyMCubes>=0.1.6   # torchmcubes shim backend for the vendored TripoSR code

# --- Background removal (rembg[gpu] pulls onnxruntime-gpu; CPU fallback automatic) ---
rembg[gpu]>=2.0.57

# --- Head-pose estimation for multi-view keyframe selection (optional; the
#     pipeline falls back to temporal sampling when mediapipe is missing) ---
mediapipe>=0.10

# --- Generative backends (TripoSR / Stable Fast 3D / Depth Anything) ---
transformers>=4.40
einops>=0.7
omegaconf>=2.3
safetensors>=0.4
huggingface_hub>=0.23
timm>=1.0
accelerate>=0.30

# --- GUI ---
gradio>=4.44

# --- Dev ---
pytest>=8.0
